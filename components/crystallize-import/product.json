{
    "title": "Product",
    "description": "A product in the catalog",
    "type": "object",
    "properties": {
        "name": {
            "description": "The name of the product",
            "type": "string",
            "minLength": 5,
            "maxLength": 255
        },
        "components": {
            "description": "The story telling",
            "type": "object",
            "properties": {
                "title": {
                    "description": "The title of the story",
                    "type": "string",
                    "minLength": 5,
                    "maxLength": 255
                }
            },
            "required": ["title"]
        },
        "variants": {
            "description": "The product variants",
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "sku": {
                        "description": "The stock keeping unit for the product",
                        "type": "string",
                        "minLength": 3,
                        "maxLength": 50
                    }
                },
                "required": ["sku"]
            }
        }
    },
    "required": ["name", "components", "variants"]
}
